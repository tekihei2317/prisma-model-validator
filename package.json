{
  "private": true,
  "name": "@tekihei2317/prisma-model-validator-workspaces",
  "workspaces": [
    "prisma-model-validator",
    "examples/*"
  ],
  "scripts": {
    "lint": "yarn run lint:tsc && yarn run lint:prettier",
    "lint:tsc": "tsc --noEmit",
    "lint:prettier": "prettier --check '**/*/*.{ts,tsx}' --ignore-path .gitignore",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "jest": "^29.4.3",
    "prettier": "^2.8.4",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "license": "MIT"
}
